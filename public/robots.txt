User-agent: *
Allow: /
Disallow: /api/

# Private and authenticated application routes are intentionally not blocked
# here because they return X-Robots-Tag: noindex, nofollow, noarchive via
# public/_headers. Crawlers must be able to request those URLs to observe
# the noindex directive and remove legacy search results. Access control is
# enforced by the application, not by robots.txt.

Sitemap: https://kriptoaman.com/sitemap.xml
