# KAM Mainnet Readiness Audit — 2026-08-24

Status: **MAINNET CANDIDATE / NOT YET PUBLIC**

This checkpoint records the latest verified public evidence and the remaining production gates for KriptoAman Mainnet. It intentionally does not promote the network or change `network-profile.json`.

## Network baseline

- Network: KriptoAman Mainnet
- Native symbol: KAM
- Decimals: 18
- Chain ID: 22028 (`0x560c`)
- Execution model: EVM-compatible
- Consensus target: QBFT
- Validator target: 4 persistent production validators
- Public RPC: `https://rpc.kriptoaman.com`
- Explorer: `https://explorer.kriptoaman.com`

## Verified public evidence already archived

Issue #115 records a passing KAM Mainnet Promotion Gate with `ready: true` for the public endpoint checks. The archived evidence confirms:

- RPC returned the expected Chain ID `0x560c`.
- Block height advanced during independent probes.
- Explorer and RPC tracked the same chain within the configured block-distance tolerance.
- RPC-sensitive namespaces (`admin_*`, `debug_*`, `personal_*`, `qbft_*`) were blocked from the public surface.
- RPC and explorer were reachable over HTTPS.
- Hourly external uptime/latency/security monitoring was enabled.

These public checks are necessary but are not sufficient to declare public-mainnet status.

## Production gates still requiring direct infrastructure evidence

The following items remain open until evidence is produced from the actual protected production environment:

- [ ] Four persistent Linux validator hosts use unique production keys.
- [ ] The private QBFT validator set reports exactly the intended four production validators.
- [ ] At least three private peers are visible from the protected evidence host.
- [ ] Validator administrative/RPC interfaces remain private and are not publicly exposed.
- [ ] Cloudflare/public RPC gateway uses a protected `KAM_RPC_ORIGIN` or equivalent protected sentry/origin path.
- [ ] The protected self-hosted runner labelled `kam-mainnet-evidence` is registered on the private operations network.
- [ ] A real isolated backup/restore test is completed and current redacted evidence is recorded.
- [ ] `KAM Private Mainnet Evidence` produces `ready: true` on the protected runner.
- [ ] At least 24 consecutive hours of passing block-production/public-readiness evidence are archived.
- [ ] Chain ID `22028` is rechecked against public EVM chain registries immediately before publication.
- [ ] Final network docs, logo and official verification channels are reviewed for publication.

## Zero-downtime rule

Do **not** create a new genesis, reset Chain ID 22028, replace chain history, or expose validator management endpoints merely to satisfy the evidence gates. Production validators should join and reconcile against the existing chain using the previously prepared zero-downtime rollout procedure.

## Promotion rule

Only after every Issue #115 gate is complete and reviewed should a separate reviewed change promote the repository status from `mainnet-candidate-not-public` to the final public-mainnet status and publish external registry metadata.

## External registry preparation

The intended registry metadata remains:

- Chain name: `KriptoAman Mainnet`
- Chain ID: `22028`
- Native currency: `KAM`
- Native decimals: `18`
- RPC: `https://rpc.kriptoaman.com`
- Explorer: `https://explorer.kriptoaman.com`

Submission to ChainList/chain registries, wallet registries, or market-data platforms must happen only after the production gates above pass and the Chain ID conflict check is repeated immediately before publication.

## Safety and claims

This audit does not claim exchange listing, token market price, regulatory approval, custody availability, staking availability, bridge availability, guaranteed uptime, or commercial launch. It records engineering readiness only.
